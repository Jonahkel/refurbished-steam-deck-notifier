[project]
name = "refurbished-steam-deck-notifier"
version = "0.1.0"
description = "description.txt"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["requests", "discord_webhook", "dotenv"]
